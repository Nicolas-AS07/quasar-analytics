# ====================================
# CONFIGURAÇÃO DO QUASAR ANALYTICS
# ====================================

# ---------------
# Google Sheets
# ---------------
# Caminho ABSOLUTO para o JSON da Service Account (NÃO commite este arquivo!)
# Exemplo: C:\Users\SeuNome\secrets\quasar-service-account.json
GOOGLE_SERVICE_ACCOUNT_CREDENTIALS_PATH=C:\\segredos\\gcp\\service_account.json

# ID da pasta no Google Drive contendo as planilhas
# Exemplo: 1a2b3c4d5e6f7g8h9i0j
SHEETS_FOLDER_ID=

# IDs adicionais de planilhas (separados por vírgula, opcional)
# Exemplo: 1abc123,2def456
SHEETS_IDS=

# Range de colunas a ler (padrão: A:ZZZ)
SHEET_RANGE=A:ZZZ

# ---------------
# Google AI Studio (Gemini API)
# ---------------
# Chave da API do Google AI Studio (obtenha em: https://aistudio.google.com/app/apikey)
ABACUS_API_KEY=

# Modelo a usar (modelos disponíveis: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash)
MODEL_NAME=gemini-2.0-flash-exp

# Temperatura (0.0 = determinístico, 1.0 = criativo)
# Recomendado: 0.3 para análise de dados
TEMPERATURE=0.3

# Máximo de tokens na resposta (AUMENTADO para RAG)
# Antes: 1000 | Novo: 4096
MAX_TOKENS=4096

# ---------------
# RAG (Retrieval-Augmented Generation)
# ---------------
# Número de resultados da busca semântica (antes: 5)
TOP_K_RESULTS=15

# Diretório para persistir ChromaDB
CHROMA_PERSIST_DIR=./data/chroma_db

# Modelo de embeddings
# Opções:
#   - all-MiniLM-L6-v2 (leve, rápido, ~80MB) [RECOMENDADO]
#   - all-mpnet-base-v2 (melhor qualidade, ~420MB)
#   - paraphrase-multilingual-MiniLM-L12-v2 (multilíngue, ~420MB)
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Habilitar RAG (True/False)
# Se False, usa busca tradicional por keywords
ENABLE_RAG=True

# Habilitar reindexação automática baseada em hash
# Se True, só reindexa quando dados mudarem
ENABLE_SMART_CACHE=True

# ---------------
# Prompt do Sistema
# ---------------
# Caminho para arquivo de prompt customizado (opcional)
# Se não definido, usa SYSTEM_PROMPT_V2 de app/prompts.py
SYSTEM_PROMPT_PATH=

# Usar prompt V2 otimizado (True/False)
USE_SYSTEM_PROMPT_V2=True

# ---------------
# Cache & Performance
# ---------------
# Habilitar TTL automático para recarga de planilhas (True/False)
SHEETS_TTL_ENABLED=True

# Intervalo de recarga em segundos (padrão: 300 = 5 minutos)
SHEETS_TTL_SECONDS=300

# Tamanho do batch para indexação (menor = menos RAM, mais lento)
INDEXING_BATCH_SIZE=100
